# hosts/cipherlogs-arch — this machine's overlay (Manjaro, i3)

Stowed last by `install.sh`, so files here win over the shared roots.

Belongs here: monitor layout (`.screenlayout/`), `mimeapps.list`,
`user-dirs.*`, `autostart/`, `environment.d/` real files, systemd user
units that differ per host. Secrets still never committed — templates
in the shared roots, real files local-only.
