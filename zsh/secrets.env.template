# Copy to secrets.env (chmod 600) and fill in — never commit the real file.
export MODEL_API_KEY="YOUR_MODEL_KEY"
export GITHUB_PAT_TOKEN="YOUR_GITHUB_PAT"
export FINGERPRINT_MCP_KEY="YOUR_FINGERPRINT_KEY"
